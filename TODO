
LEFT
- heredoc

- wait (rewrite) & exit status

- builtin: export

- norminette


ERRORS
needs FIXING (output not correct) (printed twice)
-- < infile | wc
-- ls > nonexisting file - la
-- grep 'minishell' < input.txt | sort > sorted.txt


FIX STYLE
 - ft_execution
 - add headers everywhere